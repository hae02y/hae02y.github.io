---
title: 투게독 백엔드 컨벤션
tags:
  - TogeDog
style: fill
color: secondary
comments: true
description: 백엔드 컨밴션을 정리한 문서입니다.
---

## 업무 분담

해영 : JWT, OAUTH, Meber Domain
재우 : 채팅기능, 알람기능, Map 기능
한준 : Feed, admin, 팔로우, S3

# 컨벤션

0. 대지향점

## 목표

1. 객체지향 설계 5대 원칙 (**SOLID**, SRP(단일 책임 원칙), OCP(개방-폐쇄 원칙), LSP(리스코프 치환 원칙), ISP(인터페이스 분리 원칙), DIP(의존 역전 원칙)) 에 대한 고민
2. 지속 가능하고 확장하기 쉬운 아키텍처와 코드에 대한 고민
3. 세밀한 테스트 케이스 작성으로 기능에 대한 확신을 가지기

## 개발 문화

1. 수정에는 관대하고, 오류와 모르는 건 배우는 자세로
    1. pre-project 는 배우는 단계이니 모르는 건 학습한다는 생각으로
2. 소통을 통한 발전
    1. 하나의 팀이라는 생각으로 원활한 소통이 있었으면 좋겠습니다.
    2. 부족한 부분은 바로바로 이야기를 나누고 피드백
    3. 도입하고자 하는 기술에 대해서는 서로의 생각과 근거를 명확히 제시하면서 의견을 교환
    
     
    

# 1. 개발 툴

## Aplication

1. JAVA 11버전
2. SpringBoot 2.7.14 
    1. Srping Data JPA
    2. Querydsl (필요 시)
    3. Spring Security (OAuth 2.0)
3. API 문서화 : Rest Docs

## DB

1. 로컬 및 테스트 : **H2**
2. 개발 및 운영 서버 : **MySQL (aws)** 

## DB 설계

1. **Workbench** 사용 (예시입니다.)
    
    ![Untitled](https://prod-files-secure.s3.us-west-2.amazonaws.com/563d0d5d-79c6-435e-891a-14f4022d574d/c5e21915-6380-4dbc-beba-1aa225928fef/Untitled.png)
    

## AWS

![Untitled](https://prod-files-secure.s3.us-west-2.amazonaws.com/563d0d5d-79c6-435e-891a-14f4022d574d/7ec2cb9d-0698-4927-81d5-e9ff9e5c8b41/Untitled.png)

# 2. 디렉토리 구조

1. 기본 디렉토리 구조
    
    ```java
    📦server
     ┣ 📂gradle/wrapper
     ┣ 📂src
     ┃ ┣ 📂main
     ┃ ┃ ┣ 📂java/sixman/stackoverflow
     ┃ ┃ ┃ ┣ 📂auth //인증, 인가 처리에 관한 모든 사항, 인증 정보(Authentication) 에 관한 Util 클래스
     ┃ ┃ ┃ ┣ 📂global //각 모듈 공통 사항
     ┃ ┃ ┃ ┃ ┣ 📂config
     ┃ ┃ ┃ ┃ ┣ 📂entity
     ┃ ┃ ┃ ┃ ┣ 📂aop
     ┃ ┃ ┃ ┃ ┣ 📂exception
     ┃ ┃ ┃ ┃ ┃ ┣ 📂businessexception
     ┃ ┃ ┃ ┃ ┃ ┗ 📂exceptionhandler //@RestControllerAdvice
     ┃ ┃ ┃ ┃ ┗ 📂response
     ┃ ┃ ┃ ┣ 📂domain //개별 도메인
     ┃ ┃ ┃ ┃ ┣ 📂post
     ┃ ┃ ┃ ┃ ┃ ┣ 📂controller
     ┃ ┃ ┃ ┃ ┃ ┣ 📂entity
     ┃ ┃ ┃ ┃ ┃ ┣ 📂repository
     ┃ ┃ ┃ ┃ ┃ ┗ 📂service
     ┃ ┃ ┃ ┃ ┣ 📂comment
     ┃ ┃ ┃ ┃ ┗ 📂like
     ┃ ┃ ┗ 📂resources //yml 파일로 작성, messages.properties 는 properties
     ┗ ┗ 📂test
    ```
    
2. 설정한 디렉토리 외 필요 시 반드시 **팀장에게 알려주고 추가**해주세요.

# 3. 세부 클래스 (공통)

## Entity

 1. 테이블 명 == Entity 명

1. 각 도메인 당 하나 이상의 Entity 를 가짐
2. BaseEntity 설정 (created, modifed 날짜, 시간)
    
    ```java
    @MappedSuperclass
    @EntityListeners(AuditingEntityListener.class)
    @Getter
    public abstract class BaseEntity {
    
        @CreatedDate
        @Column(updatable = false)
        protected LocalDateTime createdDate;
    
        @LastModifiedDate
        protected LocalDateTime modifiedDate;
    }
    ```
    
3. BaseEnum 설정 
    1. enum 의 포맷을 똑같이 하기 위해서 사용
    
    ```java
    public interface BaseEnum {
        String getName();
        String getDescription();
    }
    ```
    

## 공통 Dto

1. **ApiDto**
    1. 응답값에 대한 공통된 양식 (에러 응답값 포함)
    
    ```java
    public class ApiDto<T> {
    	private T data; //실제 데이터
    	private int code; // 200, 400, 404
    	private String status; //"OK", "NO CONTENT"
    	private String message; // "OK", "NO CONTENT", "ID 를 입력해주세요"
    }
    
    public class PageApiDto<T> {
    	private T data; //실제 데이터
    	private PageInfo pageInfo; 
    	private int code; // 200, 400, 404
    	private String status; //"OK", "NO CONTENT"
    	private String message; // "OK", "NO CONTENT", "ID 를 입력해주세요"
    }
    ```
    
2. PageInfo
    1. 응답값이 페이지일 때 사용
    
    ```java
    public class PageInfo {
    
        private int page;
        private int size;
        private int totalPage;
        private int totalSize;
        private boolean first;
        private boolean last;
        private boolean hasNext;
        private boolean hasPrevious;
    }
    ```
    

## Util, Constant 클래스

1. 두 개 이상의 클래스에서 동일한 기능을 필요로 하면 Util, Constant 클래스로 나눕니다.
    1. 각각의 서비스 클래스에서 Authentication 에 있는 정보를 조회하는 기능이 필요하다면?
        - SecurityUtil 클래스에서 해당 기능 일임
        
        ```java
        public class SecurityUtil {
        
            public static Long getCurrentId() {
                final Authentication authentication = SecurityContextHolder.getContext().getAuthentication();
        
                CustomUserDetails userDetails = (CustomUserDetails) authentication.getPrincipal();
        
                validate(authentication);
        
                return userDetails.getId();
            }
        
            private static void validate(Authentication authentication) {
                if (authentication == null || authentication.getName() == null) {
                    throw new MemberNotFoundException();
                }
            }
        }
        ```
        
    2. 특정 상수가 계속해서 반복된다면?
        - Constant 클래스 선언해서 사용
        
        ```java
        public class AuthConstant {
        
            public static final String AUTHORIZATION = "Authorization";
            public static final String BEARER = "Bearer ";
            public static final String REFRESH = "Refresh";
            public static final String Auth_PATH = "/v1/api/auth";
            public static final String LOGIN_PATH = "/v1/api/auth/login";
            public static final String REFRESH_URL = "/v1/api/auth/refresh";
            public static final String LOCATION = "Location";
            public static final String AUTHORITIES_KEY = "auth";
        
            ....
        }
        ```
        
        - 장점 : 상수값 변경 시 한번에 적용 가능
        - 문제점 : OCP 문제, 상수 사용하는 클래스는 모두 의존성이 높아짐
        - 해결 : 도메인별, 계층별 Constant 클래스 생성 → 완전히 해결할 수 없음, Trade-off

## domain

1. 개별적인 도메인 관련 내용
    1. controller, repostiory, service 포함
    2. 각각의 dto 사용
2. DTO
    1. ControllerDto 와 ServiceDto 사용 → 확장 가능한 구조를 위해 계층 간 분리
        - 참고할만한 글 : https://tecoble.techcourse.co.kr/post/2021-04-25-dto-layer-scope/
    2. Controller 에서 도메인을 알지 못하게 하기 위해 사용(관심사 분리)
    
    ```java
    
     ┣ 📂domain
     ┣ 📂post
     ┃ ┣ 📂controller
     ┃ ┃ ┣ 📂dto
     ┃ ┃ ┃ ┗ 📂request
     ┃ ┣ 📂entity
     ┃ ┣ 📂repository
     ┃ ┣ 📂service
     ┃ ┃ ┣ 📂dto
     ┃ ┃ ┃ ┣ 📂request
     ┗ ┗ ┗ ┗ 📂response
    ```
    
    c. 명명 규칙
    
    - 요청 : [엔티티명][동작명][Api/Service][Request]
        - ex) PostCreateApiRequest, PostUpateServiceRequest, PostSearchCond
    - 응답 : [엔티티명][Response]
        - ex) PostResponse

## exception

1. 기존 방식
    - 기존 예외 처리 방식 : 예외 상황에 대한 ExceptionCode 작성 후 BusinessLogicException 의 파라미터로 넣어서 사용
    
    ![Untitled](https://prod-files-secure.s3.us-west-2.amazonaws.com/563d0d5d-79c6-435e-891a-14f4022d574d/6c179dfa-8ac4-4f91-8837-9a84f52318d4/Untitled.png)
    
    ![Untitled](https://prod-files-secure.s3.us-west-2.amazonaws.com/563d0d5d-79c6-435e-891a-14f4022d574d/2b12e109-1544-41b5-add8-a5781c9c4ab0/Untitled.png)
    
- 문제점
    - 의존성 자석 문제 → 무분별한 Enum import
    - OCP 위반 → 오류를 추가하려면 기존 클래스를 수정해야 함
1. 수정 방식 
- Business 예외의 최상위 추상 클래스 : BusinessException
- 각 도메인 및 상황에 맞는 추상 자식 클래스 (ex. MemberException)
- 도메인별 상황에 대한 구체적인 클래스 선언

```java
@Getter
public abstract class BusinessException extends RuntimeException {

    private final String errorCode;
    private final HttpStatus httpStatus;

    protected ApplicationException(String errorCode, HttpStatus httpStatus, String message) {
        super(message);
        this.errorCode = errorCode;
        this.httpStatus = httpStatus;
    }
}
--------------------------------------------------------------------------------------------------------------
public abstract class MemberException extends BusinessException {
    protected MemberException(String errorCode, HttpStatus httpStatus, String message) {
        super(errorCode, httpStatus, message);
    }
}
--------------------------------------------------------------------------------------------------------------
@Getter
public class MemberNotFoundException extends MemberException{

    public static final String MESSAGE = "탈퇴했거나 존재하지 않는 회원입니다.";
    public static final String ERROR_CODE = "MEMBER-401";

    public MemberNotFoundException() {
        super(CODE, HttpStatus.UNAUTHORIZED, MESSAGE);
    }
}
```

 

1. 효과
- OCP → 수정에는 닫혀있고 (기존 클래스 수정 불필요), 확장에는 열려있음(새로운 예외는 새로운 클래스를 정의)
- 의존성 자석 문제 해결은 미지수
1. 디렉토리 구조
    
    ![Untitled](https://prod-files-secure.s3.us-west-2.amazonaws.com/563d0d5d-79c6-435e-891a-14f4022d574d/ea3de902-c46f-4fac-a3ef-5d4c763e3f75/Untitled.png)
    

# 4. 테스트

- 모든 클래스, 메서드에 대한 단위 테스트 진행
- 예외 상황이 있다면 반드시 예외가 터지는지 테스트
- 테스트는 되도록 Edge case 로 테스트 → Edge case 란 경계선에 있는 값을 테스트
    - 예를 들어 Member 의 name 이 1~10글자라면
        - happy case 로 10글자로 테스트
        - 예외 케이스로 11글자 테스트 → 예외가 터지는지 확인
- 테스트도 **문서**다 → 읽기 편하고, 어떤 테스트인지 한 눈에 알 수 있어야 함
    - `@DisplayName` 을 활용하여 테스트의 조건, 결과를 서술
        - `@DisplayName("member 의 이름과 패스워드로 멤버를 생성한다.")`
        - `@DisplayName("member 의 이름이 중복되면 MemberNameDuplicateException 예외가 발생한다.")`
    - BDD 사용 → given, when, then

## Auth

- 기능 테스트
- `@SpringBootTest`

## Entity

- Entity 에서 생성 메서드나 비즈니스 메서드 → 모두 테스트 (예외 상황이 있다면 포함)

## Repository

- Jpa 의 기본 메서드는 테스트 x
    - ex) save(), findById()
- 직접 만든 메서드는 테스트
    - ex) findByEmail()
- `@DataJpaTest` 사용

## Service

- `@SpringBootTest` 사용
- 모든 public 메서드 테스트
- Repository 와 연계된 테스트 → mock 사용 x

## Controller

- `@WebMvcTest` 사용
- Service 는 mock 사용
- 해당 값이 정확히 오는지 테스트 → API 문서화 병행 (restDocs)
- Dto 의 validation 검증

## 테스트 코드 공통화

- 각각 Repository, Service, Controller 테스트에서 사용하는 공통적인 코드는 각각 추상 클래스로 정의하여 각 테스트가 상속하는 구조

```java
@SpringBootTest
@Transactional
public abstract class ServiceTest {

}

class PostServiceTest extends ServiceTest {

}

class MemberServiceTest extends ServiceTest {
}
```

- 기대 효과 : Test 코드 공통화, Test 실행 시 실행되는 SpringBoot 최소화

# 5. 메서드, 필드 사용

## 캡슐화

- 클래스 내 생성자, 필드 등은 모두 기본 **Private**
- 정보 교환에 필요한 내용만 protected, public 으로 열어두기

## 객체 생성 시 정적 팩토리 메서드 사용

- 생성자 사용 x → 팩토리 메서드 사용
- 객체 생성의 유연함을 제공, 객체를 생성할 때 명확한 의미 전달

```java
public class Member {
    private String name;

    private Member(String name) { //생성자는 Private
        this.name = name;
    }

    public static Member createMember(String name) { //객체 생성 로직
        // 추가적인 로직이 들어갈 수 있음
        return new Member(name);
    }
}
```

- 정적 팩토리 메서드 명명
    - 엔티티 : `create~()`
    - dto : `of()`

## 코드 작성 시 규칙

### 필드 및 메서드 명명

| 종류 | 표기법 | 조합 | 예시 |
| --- | --- | --- | --- |
| 변수 필드 | camelCase | 명사 | username,  bookId |
| 상수 필드 | UPPER_CASE |  | AGE_LIMIT |
| boolean 필드 | camelCase | is + 명사,
has + 명사 | isCancel, hasBook |
| 함수 | camelCase | 동사 + 명사 | createMember |
| 클래스 | camelCase |  |  |
| 패키지 | lowercase |  |  |

### 메서드가 한 번에 두 개 이상의 동작을 하면 기능을 분리

```java
public Member createMember(String name, String password, Boolean isAdmin){

	if(password < 8) throws new MemberPasswordException(); //password validation

	Member member = new Member(name, password);
	member.changeAuthority(ROLE.ADMIN); //member create

	memberRepository.save(member); //member save
}

---------------------------------------------------------------
public Member createMember(String name, String password, Boolean isAdmin){

	validatePassword(password);

	Member member = createMember(name, password, isAdmin);

	memberRepository.save(member); //member save
}

private void validatePassword(String password){
	if(password < 8) throws new MemberPasswordException();
}

private Member createMember(String name, String password, Boolean isAdmin){

	Member member = new Member(name, password);
	member.changeAuthority(ROLE.ADMIN); //member create
}
```

### 메서드 명명 시 주요 단어

- CRUD : create, get, update, delete
    - *멤버 생성 : createMember()*
    - *멤버 조회 : getMember()*
    - *멤버 리스트 조회 : getMembers()*
- *boolean 값 체크 : is, has*
    - *성인인지 체크 : isAdult()*
    - *책을 20권 이상 가지고 있는지 : hasBookMoreThan()*
- *비즈니스 유효성 검사 : validate + 유효성 검사 값*
    - *유효한 member : validateMember()*
- *그 밖의 메서드 네이밍은 “**네이밍으로만 메서드의 기능을 알 수 있게 작성”**합니다.*
    - *단어 선택도 통일되어야 읽기 편하니 메서드 네이밍이 고민된다면 논의*
    - *ex) 기존 값에 새로운 값을 추가하는 메서드 → add? append?*

### 메서드 선언

- ⭕
    
    ```java
    public int solution(){
    	return -1;
    }
    ```
    

- ❌
    
    ```java
    public int solution (){
    	return -1;
    }
    ```
    

### 띄어쓰기

```java
String a = "abc" // O
String a="abc" //x
a > b //o
a>b //x

a, b // o
a,b //x
a , b //x

(가, 나, 다) //o
(가,나,다) //x
```

### 파라미터 줄바꿈

- **4개 이상은 줄바꿈 필수**
- 파라미터 2개 ~ 3개 : 필요에 따라 줄바꿈
    
    ```java
    public void test(String a, String b, int c){
    	...
    } //짧아서 한 눈에 들어오기 때문에 괜찮음
    
    public void test(@RequestBody @Valid MemberUpateApi dto,
    								 @PathVariable("member-id") memberId){
    ...} //길어서 줄바꿈 필요
    
    public Member createMember(Dto dto){
    
    	return Member.createMember(
    					dto.getName(),
    					dto.getEmail(),
    					dto.getPassword()
    					)
    } //마찬가지로 한 눈에 들어오게 줄바꿈
    ```
    

### 스프링 컴포넌트의 생성자

- **명시성**을 위해 아래와 같이 사용

```java
@Service
public class Service { //⭕

	private Repository repository;

	public Service(Repository repository){
		this.repository= repository;
	}
}

@Service
@RequiredArsConstructor
public class Service { //❌

	private final Repository repository;
}
```

### Lombok 사용 및 getter, setter

- `@Getter` 는 필요한 곳에 사용
- `@Setter` 는 사용 지양 → 필드 값 변경이 필요하면 비즈니스 로직을 생성
    - 변경하면 안되는 값도 변경을 시도하려고 할 수 있기 때문 → 캡슐화
    
    ```java
    class Member {
    	private Long id;
    	private String name;
    	private String password;
    
    	public changeName(String name){
    		//이름을 변경하는 로직
    	}
    }
    ```
    
- `@~~ArgsConstructor` 는 필요할 때만 사용 → **필요한 생성자만 만들어놓고 private 으로 막음**
    - 객체 생성은 create~ 나 of 등 static 클래스 사용 → 비즈니스 로직을 거치지 않은 무분별한 생성 방지
    
    ```java
    @AllArgsConstructor(access=PRIVATE)
    class Member {
    	private Long id;
    	private String name;
    	private String password;
    
    	private Member(String name, String password){
    		this.name = name;
    		this.password = password;
    	}
    
    	public static createMember(String name, String password){
    		return new Member(name, password);
    	}
    }
    ```
    

### 매개변수 URI Link 표기

```java
@GetMapping("/{member-id}")
public ResponseEntity getMember(@PathVariable("member-id") Long memberId){
	...
}
```

### 주석 사용

- 주석은 **반드시 필요할 때만 사용**합니다.
- **주석을 사용하지 않고도 이해할 수 있도록 코드를 작성해야 함**
- 정말 주석이 필요할 경우
    
    ```java
    //간단한 주석
    
    /*
    주석이 길 경우
    */
    
    /**
    * 메서드에 대한 설명
    */
    ```
    

# 6. 분업

- 기능 중심… 로그인 구현, 포스트 작성,

- 도메인별 개발,
- Auth 인증
- API 문서화,
- DB 설계 (도메인 클래스 설계)
- 개발 문서화

DB 설계 → 도메인 작성 → Repository → Service → Controller

API 문서화 → 대충 적어놓고, StubController 로 만들어서 Restdocs

Security → disable 해놓은 다음에 개발 → 테스트 → 테스트 완료되면 enable

# 7. 추가 사항

- 일정 시작 1시간 전 일정 재확인
- 디스코드는 무조건 수업시간에 참여하기

공통사항

- RestDocs 문서화 준비 작업
- Exception
- ApiReponse